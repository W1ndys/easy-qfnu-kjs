# QFNU CAS 登录配置
# 复制此文件为 .env 并填写真实信息
#
# Docker Compose 部署步骤：
# 1. cp .env.example .env
# 2. 填写真实的学号和密码
# 3. docker compose up -d

QFNU_USERNAME=你的学号
QFNU_PASSWORD=你的密码

# 服务器端口（Docker 内部端口，由 Traefik 代理）
PORT=8080
# 设置为 release 以启用生产模式
GIN_MODE=release