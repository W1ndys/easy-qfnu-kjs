# QFNU CAS 登录配置
# 复制此文件为 .env 并填写真实信息
#
# Docker Compose 部署步骤：
# 1. cp .env.example .env
# 2. 填写真实的学号和密码
# 3. docker compose up -d --build

QFNU_USERNAME=你的学号
QFNU_PASSWORD=你的密码

# 后端容器监听端口，由前端容器和 Traefik 间接访问
PORT=8080
# 建议生产环境保持 release
GIN_MODE=release
